(defpackage lambda-server-test
  (:use :cl
        :lambda-server
        :prove))
(in-package :lambda-server-test)

;; NOTE: To run this test file, execute `(asdf:test-system :lambda-server)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
